# Optional. The application remains fully functional with its deterministic local fallback.
# Use an OpenRouter Qwen model ID in `qwen/<model-slug>` format.
# Choose a currently available Qwen chat model from https://openrouter.ai/qwen.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
